version: 2.1

jobs:
  terraform_init:
    docker:
      - image: hashicorp/terraform:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Terraform Init
          command: |
          set TF_VAR_region = new_region
          terraform init

  terraform_output:
    docker:
      - image: hashicorp/terraform:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Terraform Output
          command: 
            terraform apply --auto-approve
#-var-file="${ENV}.tfvars"
workflows:
  version: 2 
  Init-Apply-Output:
    jobs:
      - terraform_init
      - terraform_output :
          requires : 
            - terraform_init
          filters:
             branches:
                only: main
